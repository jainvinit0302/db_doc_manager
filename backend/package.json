{
  "name": "dbdoc-backend",
  "version": "1.0.0",
  "description": "Backend for DBDocManager project",
  "main": "dist/cli.js",
  "scripts": {
    "start": "ts-node src/cli.ts",
    "dev": "nodemon --watch src -e ts --exec ts-node src/cli.ts",
    "build": "tsc",
    "validate": "ts-node src/cli.ts validate",
    "generate": "ts-node src/cli.ts generate"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "commander": "^12.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^22.0.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
