{
  "name": "dbdoc-manager",
  "version": "0.1.0",
  "description": "DSL-driven documentation & lineage for relationalized data models",
  "main": "dist/cli.js",
  "bin": {
    "dbdoc": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "generate": "ts-node src/cli.ts generate --dir .dbdoc --out docs",
    "generate:lineage": "ts-node src/cli.ts generate --dir .dbdoc --out docs && echo '✅ Lineage JSON written to docs/artifacts/lineage.json'",
    "generate:all": "ts-node src/cli.ts generate --dir .dbdoc --out docs && echo '✅ All artifacts (CSV, ERD, Lineage) generated in docs/'"
  },
  "author": "Your Team",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "commander": "^10.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "glob": "^10.2.7",
    "js-yaml": "^4.1.0",
    "mkdirp": "^3.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/glob": "^8.1.0",
    "@types/jest": "^29.5.2",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.3.1",
    "jest": "^29.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
